- flash.each do |key, value|
  %div.fade.in{class: flash_class(key), role: 'alert'}
    = value
    %button(type="button" class="close" data-dismiss="alert" aria-label="Close")
      %span(aria-hidden="true")
        %i.fa.fa-times-circle-o
