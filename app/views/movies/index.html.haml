.row
  .col-xs-12
    %h1 Movies

    = render 'movie_filters_nav'

.row
  - @movies.each do |movie|
    .col-xs-6.col-sm-4.col-md-3.col-lg-2
      = link_to movie_path(movie.id), class: 'thumbnail' do
        = image_tag(poster_url(movie), alt: movie.title)
