.row
  .col-xs-12
    %h1.page-header Popular People

  - @people.each do |person|
    .col-xs-6.col-sm-4.col-md-3.col-lg-3
      .thumbnail
        = link_to person_path(person.id) do
          = image_tag person.profile_url, alt: person.name
        .caption
          %h5= person.name
